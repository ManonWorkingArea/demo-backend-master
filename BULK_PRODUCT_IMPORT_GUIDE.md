# คู่มือระบบเพิ่มสินค้าจำนวนมาก (Bulk Product Import)

## 📋 ภาพรวม

ระบบนี้เพิ่มความสามารถในการนำเข้าสินค้าจำนวนมากพร้อมกันในหน้า Product Form โดยใช้ modal dialog ที่รับข้อมูลจาก textarea แล้วนำไปบันทึกทีละรายการโดยอัตโนมัติ

## 🎯 คุณสมบัติ

- ✅ เพิ่มสินค้าหลายรายการพร้อมกัน (1 บรรทัดต่อ 1 รายการ)
- ✅ รูปแบบข้อมูล: `SKU[TAB]ชื่อสินค้า`
- ✅ กำหนดผู้จัดจำหน่ายเป็น `690e4ff218848ad06add19af` อัตโนมัติ
- ✅ ใช้ logic เดียวกับแบบฟอร์มปกติ (validation, code generation, etc.)
- ✅ แสดงความคืบหน้าแบบ real-time
- ✅ สรุปผลการนำเข้า (สำเร็จ/ล้มเหลว)

## 🚀 วิธีใช้งาน

### 1. เปิด Modal นำเข้าข้อมูล

ในหน้า Product Form (โหมดเพิ่มสินค้าใหม่เท่านั้น) จะมีปุ่ม **"เพิ่มหลายรายการ"** สีม่วง:

```
[← ยกเลิก] [📥 เพิ่มหลายรายการ] [🐛]
```

### 2. ใส่ข้อมูลสินค้า

ในช่อง textarea ให้ใส่ข้อมูลตามรูปแบบ:

```
406014120	RECACRIL LTDO. 14 BLANCO-VERDE
406014121	RECACRIL LTDO. 15 AZUL-BLANCO
406014122	RECACRIL LTDO. 16 ROJO-VERDE
```

**หมายเหตุ:**
- ใช้ **TAB** คั่นระหว่าง SKU กับชื่อสินค้า
- 1 บรรทัดต่อ 1 รายการสินค้า
- SKU จะถูกแปลงเป็นตัวพิมพ์ใหญ่อัตโนมัติ

### 3. ตรวจสอบตัวอย่าง

Modal จะแสดงตัวอย่าง 5 รายการแรกให้ตรวจสอบก่อนนำเข้า:

| # | SKU | ชื่อสินค้า |
|---|-----|-----------|
| 1 | 406014120 | RECACRIL LTDO. 14 BLANCO-VERDE |
| 2 | 406014121 | RECACRIL LTDO. 15 AZUL-BLANCO |

### 4. กดปุ่มนำเข้า

กดปุ่ม **"เริ่มนำเข้า (X รายการ)"** เพื่อเริ่มกระบวนการ

### 5. ติดตามความคืบหน้า

ระบบจะแสดง:
- Progress bar แสดงความคืบหน้า
- สถานะปัจจุบัน: `กำลังนำเข้า: 406014120 - RECACRIL LTDO. 14 BLANCO-VERDE`
- จำนวนที่ดำเนินการแล้ว: `5/10`

### 6. ดูผลสรุป

เมื่อเสร็จสิ้น จะแสดง:
- ✅ นำเข้าสำเร็จ: X รายการ
- ❌ ล้มเหลว: X รายการ (ถ้ามี)

## 🔧 การทำงานภายใน

### Flow การนำเข้าข้อมูล

```
1. Parse textarea → แยกข้อมูล SKU และ ชื่อสินค้า
2. Loop แต่ละรายการ:
   a. Fill form data:
      - sku: จาก input
      - product_name: จาก input
      - category: 'textile'
      - unit: 'meter'
      - supplier_id: '690e4ff218848ad06add19af'
      - status: 'active'
   
   b. Generate product_code อัตโนมัติ
   
   c. Validate form
   
   d. Create product ผ่าน InventoryService
   
   e. เก็บผลลัพธ์ (success/fail)
   
   f. รอ 200ms ก่อนรายการถัดไป

3. แสดงสรุปผลทั้งหมด
4. Reset form
```

### ข้อมูลที่ถูกตั้งค่าอัตโนมัติ

| Field | Value |
|-------|-------|
| `category` | `'textile'` |
| `unit` | `'meter'` |
| `supplier_id` | `'690e4ff218848ad06add19af'` |
| `supplier` | `'Supplier (Auto)'` |
| `status` | `'active'` |
| `unit_price` | `0` |
| `retail_price` | `0` |
| `wholesale_price` | `0` |
| `min_stock` | `5` |

## 📊 Component State

### New State Variables

```javascript
const showBulkImportModal = ref(false)      // แสดง/ซ่อน modal
const bulkImportText = ref('')              // ข้อมูลจาก textarea
const bulkImporting = ref(false)            // กำลังนำเข้าอยู่
const bulkImportProgress = ref(0)           // จำนวนที่ทำแล้ว
const bulkImportTotal = ref(0)              // จำนวนทั้งหมด
const bulkImportStatus = ref('')            // สถานะปัจจุบัน
const bulkImportResults = ref([])           // ผลลัพธ์แต่ละรายการ
```

### New Computed Properties

```javascript
bulkImportLines           // แยกข้อมูลจาก textarea
bulkImportPreview         // ตัวอย่าง 5 รายการแรก
bulkImportProgressPercent // เปอร์เซ็นต์ความคืบหน้า
bulkImportSuccessCount    // จำนวนสำเร็จ
bulkImportFailCount       // จำนวนล้มเหลว
```

### New Methods

```javascript
closeBulkImportModal()  // ปิด modal (ยืนยันถ้ากำลังนำเข้าอยู่)
clearBulkImport()       // ล้างข้อมูลทั้งหมด
processBulkImport()     // ดำเนินการนำเข้า
```

## 🎨 UI/UX Features

### Modal Design
- **Header**: gradient background สีม่วง พร้อมไอคอน
- **Instructions**: กล่องคำแนะนำสีน้ำเงิน
- **Textarea**: แสดง placeholder ตัวอย่าง
- **Preview Table**: แสดงข้อมูล 5 รายการแรก
- **Progress Bar**: gradient animation แสดงความคืบหน้า
- **Results**: กล่องสรุปผลสีเขียว

### Responsive
- ใช้ Tailwind CSS
- รองรับหน้าจอทุกขนาด
- Modal เต็มหน้าจอบนมือถือ

## ⚠️ ข้อควรระวัง

1. **ปุ่มจะแสดงเฉพาะโหมดเพิ่มสินค้าใหม่เท่านั้น** (`mode === 'add'`)
2. **ใช้ TAB ในการคั่นข้อมูล** ไม่ใช่ช่องว่าง
3. **SKU ต้องไม่ซ้ำ** - ระบบจะ error ถ้า SKU ซ้ำ
4. **Validation เหมือนฟอร์มปกติ** - SKU ต้องเป็นตัวอักษรและตัวเลข
5. **มีการหน่วงเวลา 200ms ระหว่างรายการ** เพื่อไม่ให้ทำ database ทำงานหนักเกินไป

## 🐛 Troubleshooting

### ปัญหา: SKU ไม่ถูกต้อง
**วิธีแก้:** ตรวจสอบให้แน่ใจว่า SKU เป็นตัวอักษรภาษาอังกฤษและตัวเลขเท่านั้น

### ปัญหา: ไม่มีชื่อสินค้า
**วิธีแก้:** ตรวจสอบว่าใช้ TAB คั่นระหว่าง SKU และชื่อสินค้า

### ปัญหา: นำเข้าล้มเหลว
**วิธีแก้:** ดูใน console log (`❌ [X/Y] Failed:`) เพื่อดูรายละเอียด error

## 📝 ตัวอย่างข้อมูลที่ใช้งานได้

```
406014120	RECACRIL LTDO. 14 BLANCO-VERDE
406014121	RECACRIL LTDO. 15 AZUL-BLANCO
406014122	RECACRIL LTDO. 16 ROJO-VERDE
406014123	RECACRIL LTDO. 17 AMARILLO-NEGRO
406014124	RECACRIL LTDO. 18 NARANJA-GRIS
```

## 🔍 Console Logs

ระบบจะ log ข้อมูลเพื่อ debug:

```
🚀 Starting bulk import: 5 items
📦 [1/5] Processing: 406014120
✅ [1/5] Success: 406014120
📦 [2/5] Processing: 406014121
...
🏁 Bulk import completed: { successCount: 5, failCount: 0 }
```

## 🎯 Use Cases

### 1. นำเข้าสินค้าผ้าจาก Recasens
```
406014120	RECACRIL LTDO. 14 BLANCO-VERDE
406014121	RECACRIL LTDO. 15 AZUL-BLANCO
```

### 2. นำเข้าสินค้าจาก Excel/CSV
1. Copy คอลัมน์ SKU และ Name จาก Excel
2. Paste ลงใน textarea
3. กดนำเข้า

### 3. นำเข้าจาก Database Export
```sql
SELECT sku, product_name FROM products;
```
→ Export เป็น TSV → Paste

## 📚 Related Files

- `/src/extensions/modules/erp/modules/inventory/components/products/shared/Form.vue` - Main form component with bulk import

## ✨ Future Enhancements

- [ ] รองรับ CSV file upload
- [ ] ตรวจสอบ SKU ซ้ำก่อนนำเข้า
- [ ] แก้ไขข้อมูลก่อนนำเข้า
- [ ] Export ผลลัพธ์เป็น CSV
- [ ] undo/redo bulk import
- [ ] กำหนด supplier ได้ในหน้า modal

---

**Version:** 1.0  
**Last Updated:** November 25, 2025  
**Author:** AI Assistant
