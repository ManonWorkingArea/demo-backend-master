# คู่มือการใช้งานระบบเลือกลูกค้าในใบขายใหม่

## 📋 สรุปการพัฒนา

ได้ทำการปรับปรุงฟอร์มสร้างใบขายใหม่ (`SalesOrderForm.vue`) โดยเพิ่มระบบเลือกลูกค้าแบบ dropdown พร้อมการค้นหา ที่มีลักษณะเหมือนกับระบบเลือก supplier ใน ProductForm

## ✨ ฟีเจอร์ใหม่

### 1. ระบบเลือกลูกค้าแบบ Smart Dropdown
- **ค้นหาแบบ Real-time**: พิมพ์ชื่อลูกค้า, รหัสลูกค้า, เบอร์โทร, อีเมล
- **แสดงข้อมูลครบถ้วน**: ชื่อ, รหัส, ผู้ติดต่อ, เบอร์โทร, สถานะ, กลุ่มลูกค้า
- **สร้างลูกค้าใหม่อัตโนมัติ**: หากไม่พบลูกค้าในระบบ จะเสนอให้สร้างใหม่
- **Auto-fill ข้อมูล**: เมื่อเลือกลูกค้าแล้ว จะเติมข้อมูลโทรศัพท์และที่อยู่อัตโนมัติ

### 2. การจัดการข้อมูลลูกค้า
- **โหลดจากระบบ Customer**: เชื่อมต่อกับ Transaction Engine
- **สถานะลูกค้า**: แสดงเฉพาะลูกค้าที่ active
- **การจัดกลุ่ม**: แสดงกลุ่มลูกค้า (ปลีก, ส่ง, VIP, ฯลฯ)
- **UI/UX เหมือน ProductForm**: ใช้สไตล์และ pattern เดียวกัน

## 🚀 วิธีการใช้งาน

### ขั้นตอนการสร้างใบขาย

1. **เลือกลูกค้า**
   ```
   - คลิกในช่อง "เลือกลูกค้า"
   - พิมพ์ชื่อหรือรหัสลูกค้าเพื่อค้นหา
   - เลือกจากรายการที่แสดง
   - หรือสร้างลูกค้าใหม่หากไม่พบ
   ```

2. **ตรวจสอบข้อมูล**
   ```
   - ข้อมูลโทรศัพท์และที่อยู่จะถูกเติมอัตโนมัติ
   - สามารถแก้ไขได้หากต้องการ
   ```

3. **เลือกสินค้าและบันทึก**
   ```
   - ดำเนินการตามปกติ
   - ระบบจะบันทึกข้อมูลลูกค้าที่เลือกพร้อมใบขาย
   ```

## 🔧 การทำงานของระบบ

### การโหลดข้อมูลลูกค้า
```javascript
// โหลดจาก Transaction Engine
const result = await engine.list('customer')

// Fallback ไปที่ localStorage
const customerKey = `transaction_customer`
const storedData = localStorage.getItem(customerKey)
```

### การสร้างลูกค้าใหม่
```javascript
const newCustomerData = {
  subtype: 'customer_master',
  customer_code: 'CUS' + timestamp,
  customer_name: customerName,
  status: 'active',
  customer_type: 'individual',
  customer_group: 'retail'
}
```

### การบันทึกใบขาย
```javascript
orderData = {
  customerId: selectedCustomer?.id || generated_id,
  customerName: selectedCustomer?.customer_name || manual_name,
  customer_code: selectedCustomer?.customer_code,
  // ... ข้อมูลอื่นๆ
}
```

## 📱 UI Components

### Customer Search Input
- **Input Field**: พร้อม icon และ placeholder
- **Dropdown**: แสดงรายการลูกค้าแบบ smart
- **Selection Display**: แสดงลูกค้าที่เลือกพร้อมปุ่มล้าง

### Customer Option Card
- **Customer Info**: ชื่อ, รหัส, ผู้ติดต่อ, โทรศัพท์
- **Status Badge**: สถานะลูกค้า (ใช้งาน, ระงับ, ฯลฯ)
- **Group Badge**: กลุ่มลูกค้า (ปลีก, ส่ง, VIP)
- **New Customer Option**: สำหรับสร้างลูกค้าใหม่

## 🎨 การออกแบบ

### สีและธีม
- **Primary**: #3b82f6 (Blue)
- **Success**: #10b981 (Green) 
- **Warning**: #f59e0b (Orange)
- **Neutral**: #64748b (Gray)

### Animation
- **Dropdown Slide In**: 0.2s ease-out
- **Hover Effects**: 0.2s transition
- **Status Indicators**: Color-coded

## 📋 การทดสอบ

### Test Cases

1. **ทดสอบการค้นหาลูกค้า**
   ```
   ✅ ค้นหาด้วยชื่อลูกค้า
   ✅ ค้นหาด้วยรหัสลูกค้า  
   ✅ ค้นหาด้วยเบอร์โทรศัพท์
   ✅ ค้นหาด้วยอีเมล
   ✅ แสดง "ไม่พบข้อมูล" เมื่อไม่มีผลลัพธ์
   ```

2. **ทดสอบการเลือกลูกค้า**
   ```
   ✅ เลือกลูกค้าจากรายการ
   ✅ Auto-fill ข้อมูลโทรศัพท์และที่อยู่
   ✅ แสดงข้อมูลลูกค้าที่เลือก
   ✅ ล้างการเลือกได้
   ```

3. **ทดสอบการสร้างลูกค้าใหม่**
   ```
   ✅ สร้างลูกค้าใหม่เมื่อไม่พบในระบบ
   ✅ สร้างรหัสลูกค้าอัตโนมัติ
   ✅ บันทึกลงในระบบ Customer
   ✅ ใช้ข้อมูลใหม่ในใบขาย
   ```

4. **ทดสอบการบันทึกใบขาย**
   ```
   ✅ บันทึกข้อมูลลูกค้าที่เลือกแล้ว
   ✅ บันทึกข้อมูลลูกค้าที่สร้างใหม่
   ✅ Validation ข้อมูลที่จำเป็น
   ✅ แสดงข้อผิดพลาดได้ถูกต้อง
   ```

### การตรวจสอบ Console
```javascript
// ดู log การโหลดลูกค้า
console: "🔄 Loading customers for sales order..."
console: "✅ Loaded customers from engine: X"

// ดู log การสร้างลูกค้าใหม่
console: "🆕 Creating new customer: [name]"
console: "✅ Customer created successfully"

// ดู log การบันทึกใบขาย
console: "🔍 Sales Order Data to be saved:"
console: "✅ Sales Order saved successfully"
```

## 🔗 ไฟล์ที่เกี่ยวข้อง

### หลัก
- `SalesOrderForm.vue` - ฟอร์มสร้างใบขายที่ปรับปรุงแล้ว
- `customer/Manager.vue` - หน้าจัดการลูกค้า
- `customer/schema.js` - โครงสร้างข้อมูลลูกค้า

### อ้างอิง
- `ProductForm.vue` - ต้นแบบการออกแบบ supplier selection
- `TransactionEngine` - สำหรับจัดการข้อมูล
- `CodeManager` - สำหรับสร้างรหัสอัตโนมัติ

## 🚀 การใช้งานจริง

### เริ่มต้นใช้งาน
1. ไปที่หน้า Sales → สร้างใบขายใหม่
2. ทดลองพิมพ์ชื่อลูกค้าในช่อง "เลือกลูกค้า"
3. เลือกจากรายการหรือสร้างใหม่
4. ตรวจสอบการ auto-fill ข้อมูล
5. สร้างใบขายตามปกติ

### แนะนำการใช้งาน
- ใช้ระบบค้นหาเพื่อหาลูกค้าอย่างรวดเร็ว
- ตรวจสอบสถานะและกลุ่มลูกค้าก่อนเลือก
- สร้างลูกค้าใหม่เมื่อจำเป็น
- ตรวจสอบข้อมูลที่ auto-fill แล้ว

## ✅ สรุป

ระบบเลือกลูกค้าใหม่ได้รับการออกแบบให้ใช้งานง่าย รวดเร็ว และมีประสิทธิภาph โดยใช้ pattern เดียวกับระบบเลือก supplier ที่มีอยู่แล้ว ทำให้ผู้ใช้งานคุ้นเคยและสามารถใช้งานได้ทันที

🎯 **การพัฒนาสำเร็จแล้ว**: ระบบ Customer Integration สำหรับ Sales Order Form พร้อมใช้งาน!