# Storage Recovery System - คู่มือการใช้งาน

## 📋 ภาพรวม
ระบบกู้คืนข้อมูล localStorage สำหรับ ERP Core ที่ถูกพัฒนาขึ้นเพื่อจัดการกับปัญหา JSON parsing error และข้อมูลที่เสียหายใน localStorage

## 🔧 การติดตั้งและใช้งาน

### 1. ไฟล์ที่ได้รับการปรับปรุง

#### `/src/extensions/modules/erp/core/drivers/LocalStorageDriver.js`
- ✅ เพิ่มการจัดการ JSON parsing error 
- ✅ ระบบสำรองข้อมูลอัตโนมัติเมื่อเกิดความเสียหาย
- ✅ การกู้คืนข้อมูลบางส่วนจาก JSON ที่เสียหาย
- ✅ การจัดการ corrupted backup files

#### `/src/extensions/modules/erp/core/utils/StorageDebugHelper.js`
- ✅ เครื่องมือตรวจสอบสุขภาพ localStorage
- ✅ ระบบรายงานความเสียหาย
- ✅ การทำความสะอาดข้อมูลเก่า
- ✅ Export/Import ข้อมูล

### 2. เครื่องมือทดสอบ

#### `/storage-recovery-test.html`
- 🖥️ Interface สำหรับทดสอบระบบ
- 📊 แสดงสถานะและความเสียหายแบบ real-time
- 🔧 เครื่องมือกู้คืนและจัดการข้อมูล

## 🚀 วิธีการแก้ปัญหา JSON Parsing Error

### ปัญหาที่เกิดขึ้น:
```
Failed to load inventory transactions from localStorage: SyntaxError: Unterminated string in JSON at position 2491
```

### วิธีแก้ไข:

#### 1. แก้ไขอัตโนมัติ (Auto Recovery)
```javascript
// ระบบจะจัดการอัตโนมัติเมื่อโหลดข้อมูล
const data = driver.loadData('inventory') // จะจัดการ corruption เองอัตโนมัติ
```

#### 2. การกู้คืนด้วยตนเอง
```javascript
import { StorageDebugHelper } from './src/extensions/modules/erp/core/utils/StorageDebugHelper.js'

const debugHelper = new StorageDebugHelper()

// ตรวจสอบสุขภาพ
const report = await debugHelper.generateHealthReport()

// ทำความสะอาดและกู้คืน
const result = await debugHelper.cleanupCorruption()
```

#### 3. ใช้เครื่องมือ Web Interface
1. เปิดไฟล์ `storage-recovery-test.html` ในเบราว์เซอร์
2. คลิก "ตรวจสอบสุขภาพ" เพื่อดูสถานะ
3. คลิก "ทำความสะอาด" เพื่อกู้คืนข้อมูลอัตโนมัติ

## 📊 คุณสมบัติของระบบ

### ⚡ การจัดการข้อมูลที่เสียหาย
- **Auto Detection**: ตรวจจับ JSON corruption อัตโนมัติ
- **Backup Creation**: สร้างสำรองข้อมูลเสียหายก่อนล้าง
- **Partial Recovery**: พยายามกู้คืนข้อมูลบางส่วนที่ยังใช้ได้
- **Clean Fallback**: ล้างข้อมูลเสียหายและเริ่มใหม่หากกู้คืนไม่ได้

### 🔍 การตรวจสอบและรายงาน
- **Health Check**: ตรวจสอบสถานะ localStorage ทั้งหมด
- **Corruption Report**: รายงานความเสียหายแบบละเอียด
- **Storage Statistics**: สถิติการใช้งานและขนาดข้อมูล
- **Recommendations**: ข้อเสนะนำการแก้ไขปัญหา

### 💾 การจัดการข้อมูล
- **Export All Data**: ส่งออกข้อมูลทั้งหมดเป็น JSON
- **Manual Restore**: กู้คืนจาก backup เฉพาะ
- **Cleanup Old Backups**: ทำความสะอาดข้อมูลสำรองเก่า
- **Clear All Data**: ล้างข้อมูลทั้งหมด (ใช้ระวัง!)

## 🔧 การใช้งานใน Code

### ตัวอย่างการใช้งานพื้นฐาน:
```javascript
import { LocalStorageDriver } from './src/extensions/modules/erp/core/drivers/LocalStorageDriver.js'

const driver = new LocalStorageDriver()

// โหลดข้อมูล - จะจัดการ corruption อัตโนมัติ
try {
  const inventoryData = driver.loadData('inventory')
  console.log('✅ Inventory data loaded:', inventoryData)
} catch (error) {
  console.error('❌ Failed to load inventory:', error.message)
}
```

### ตัวอย่างการตรวจสอบสุขภาพ:
```javascript
import { StorageDebugHelper } from './src/extensions/modules/erp/core/utils/StorageDebugHelper.js'

const debugHelper = new StorageDebugHelper()

// ตรวจสอบสุขภาพระบบ
const healthReport = await debugHelper.generateHealthReport()

if (healthReport.corruption.hasCorruption) {
  console.warn('⚠️ Corruption detected, running cleanup...')
  const cleanupResult = await debugHelper.cleanupCorruption()
  console.log('🧹 Cleanup completed:', cleanupResult)
}
```

### ตัวอย่างการกู้คืนแบบกำหนดเอง:
```javascript
// แสดงรายการ backup ที่เสียหาย
const backups = driver.getCorruptedBackups()
console.log('📋 Corrupted backups:', backups)

// กู้คืนจาก backup เฉพาะ
if (backups.length > 0) {
  try {
    const recovered = await driver.restoreFromBackup(backups[0].key)
    console.log('✅ Recovery successful:', recovered)
  } catch (error) {
    console.error('❌ Recovery failed:', error.message)
  }
}
```

## 🎯 Console Commands

เมื่อโหลดระบบแล้ว สามารถใช้คำสั่งเหล่านี้ใน browser console:

```javascript
// ตรวจสอบสุขภาพ
await debugStorage()

// ทำความสะอาด
await cleanupStorage()

// Export ข้อมูล
await exportStorage()

// ดูข้อมูล backup ที่เสียหาย
showCorruptedBackups()

// กู้คืนจาก backup
await manualRestore('erp_inventory_transactions_corrupted_1234567890')
```

## ⚠️ ข้อควรระวัง

1. **การสำรองข้อมูล**: ระบบจะสำรองข้อมูลเสียหายอัตโนมัติ แต่แนะนำให้ export ข้อมูลสำคัญเป็นระยะ
2. **การกู้คืนบางส่วน**: ข้อมูลที่กู้คืนได้อาจไม่ครบถ้วน ตรวจสอบความสมบูรณ์หลังกู้คืน
3. **ประสิทธิภาพ**: การตรวจสอบความเสียหายจะทำงานทุกครั้งที่โหลดข้อมูล อาจใช้เวลาเพิ่มขึ้นเล็กน้อย

## 📝 Log Messages ที่สำคัญ

```
🚨 JSON corruption detected in inventory data
💾 Corrupted data backed up to: erp_inventory_transactions_corrupted_1234567890
🔧 Recovery attempt found 15 valid transactions
✅ Successfully restored 15 transactions
🗑️ Cleared corrupted inventory data
```

## 🔄 การติดตาม

ระบบจะ log ข้อมูลสำคัญไปที่ console เพื่อให้ติดตามการทำงาน:
- การตรวจพบความเสียหาย
- การสำรองข้อมูล
- ผลการกู้คืน
- การทำความสะอาด

## ✅ สรุป

ระบบกู้คืนข้อมูลนี้จะช่วย:
1. **แก้ปัญหา JSON parsing error โดยอัตโนมัติ**
2. **สำรองข้อมูลเสียหายป้องกันการสูญหาย**
3. **กู้คืนข้อมูลบางส่วนที่ยังใช้ได้**
4. **ให้เครื่องมือจัดการและตรวจสอบ**
5. **ป้องกันปัญหาซ้ำในอนาคต**

หลังจากติดตั้งระบบนี้แล้ว ปัญหา "Unterminated string in JSON at position 2491" จะได้รับการจัดการอัตโนมัติและไม่ขัดขวางการทำงานของระบบ ERP อีกต่อไป