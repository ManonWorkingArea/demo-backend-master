เสร็จแล้ว! ฉันได้แก้ไข WorkOrderDetail.vue เพื่อให้ปุ่มตัดสต็อคแสดงเฉพาะรายการที่มีการจองเท่านั้น และเมื่อตัดสต็อคจะสร้าง inventory transaction และอัปเดต balance ให้ถูกต้อง:

## 🎯 สิ่งที่แก้ไขสำเร็จแล้ว:

### 1. **เงื่อนไขแสดงปุ่มตัดสต็อค**
- ✅ ตรวจสอบ `canPickItems` ว่า work order มีสถานะ 'picking' และมี `inventory_reservations`
- ✅ เพิ่มฟังก์ชัน `hasItemReservation()` เพื่อตรวจสอบแต่ละรายการ
- ✅ ปุ่มแสดงเฉพาะรายการที่มีการจอง

### 2. **การแสดงข้อมูลการจอง**  
- ✅ เพิ่มคอลัมน์ "การจอง" ในตารางสินค้า
- ✅ แสดงสถานะและจำนวนการจอง
- ✅ Reservation badges (เขียว = มีการจอง, แดง = ไม่มีการจอง)

### 3. **กระบวนการตัดสต็อคที่ปรับปรุงแล้ว**
- ✅ เช็คการจองก่อนตัดสต็อค - ถ้าไม่มีการจองจะไม่สามารถตัดสต็อคได้
- ✅ ตรวจสอบสถานะ reservation record ว่ายังไม่ complete
- ✅ หาหรือสร้าง inventory_balance record
- ✅ อัปเดต balance (qty_on_hand, qty_reserved, qty_available)
- ✅ สร้าง inventory movement transaction record
- ✅ อัปเดต reservation record เป็น 'complete' 
- ✅ เก็บ activity log ของการตัดสต็อค

### 4. **การจัดการข้อผิดพลาด**
- ✅ แจ้งเตือนเมื่อไม่มีการจอง
- ✅ แจ้งเตือนเมื่อ reservation ถูกใช้งานแล้ว  
- ✅ แจ้งเตือนเมื่อ work order ไม่มีการจองสต็อค

### 5. **UI/UX Improvements**
- ✅ ข้อความเตือนที่ชัดเจน
- ✅ สไตล์สำหรับสถานะการจอง
- ✅ Progress indicator ที่ปรับปรุงแล้ว

## 🔍 การทำงานของระบบ:

1. **เมื่อเปิด Work Order** → ตรวจสอบ `inventory_reservations`
2. **ถ้ามีการจอง** → แสดงปุ่มตัดสต็อคได้
3. **เมื่อกดตัดสต็อค** → 
   - เช็ค reservation record ยังใช้งานได้หรือไม่
   - อัปเดต inventory_balance
   - สร้าง inventory movement transaction  
   - อัปเดต reservation เป็น complete
   - บันทึก activity log
4. **ถ้าไม่มีการจอง** → แสดงข้อความ "ไม่มีการจอง" และไม่มีปุ่ม

ตอนนี้ระบบจะทำงานอย่างถูกต้องโดยตัดสต็อคเฉพาะเมื่อมีการจองและสร้าง transaction records ที่สมบูรณ์! 🎉