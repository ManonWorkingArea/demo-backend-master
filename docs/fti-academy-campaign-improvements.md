# การปรับปรุง FTI Academy Campaign Data Aggregation

## การเปลี่ยนแปลงหลัก

### 🔄 **ปรับปรุง Aggregation Pipeline**

เปลี่ยนจากการดึงเฉพาะผู้ที่มี `analytics.status` เป็นการดึงข้อมูลผู้ลงทะเบียนทั้งหมดในหลักสูตรของแคมเปญ:

#### **เดิม (Old Pipeline):**
```javascript
{
  $match: {
    courseID: { $in: campaignCourseIds },
    'analytics.status': { $exists: true } // จำกัดเฉพาะคนที่มี analytics
  }
}
```

#### **ใหม่ (New Pipeline):**
```javascript
{
  $match: {
    courseID: { $in: campaignCourseIds } // ดึงทุกคนที่ลงทะเบียน
  }
}
```

### 📊 **ข้อมูลที่เพิ่มขึ้น**

1. **`enrolledCourses`** - จำนวนหลักสูตรที่ลงทะเบียนจริง
2. **`hasAnalytics`** - ตรวจสอบว่ามีข้อมูล analytics หรือไม่
3. **`firstEnrollment`** - วันที่ลงทะเบียนแรกสุด
4. **`lastActivity`** - กิจกรรมล่าสุด
5. **`isEnrolled`** - สำหรับแต่ละหลักสูตร ระบุว่าลงทะเบียนหรือไม่

### 🎯 **การจัดกลุ่มและเรียงลำดับ**

```javascript
// เรียงลำดับตามจำนวนหลักสูตรที่ลงทะเบียน (มากไปน้อย)
{
  $sort: {
    totalEnrolledCourses: -1,
    firstEnrollment: 1
  }
}
```

### 📈 **การคำนวณสถานะใหม่**

สถานะจะถูกกำหนดตาม:
- **`completed`**: เรียนครบทั้ง 5 หลักสูตร
- **`near_completion`**: เรียนจบ ≥ 3 หลักสูตร หรือ ลงทะเบียนครบ 5 หลักสูตร + ความคืบหน้า ≥ 60%
- **`in_progress`**: ลงทะเบียน ≥ 2 หลักสูตร หรือ ความคืบหน้า ≥ 20%
- **`just_started`**: อื่นๆ

### 🔍 **ข้อมูลที่แสดงในแต่ละหลักสูตร**

```javascript
courseProgress: [
  {
    courseId: 1,
    actualCourseId: '67ebb7a72740d3df3f37dc69',
    progress: 75,
    status: 'in_progress',
    completedAt: null,
    enrolledAt: '2025-09-26',
    courseName: 'องค์กรสุขภาวะในโครงการป้องกันโรค',
    isEnrolled: true // ใหม่! ระบุว่าลงทะเบียนหรือไม่
  }
]
```

### 📋 **การแสดงผลใน UI**

#### **รายการผู้เข้าร่วม:**
- แสดงจำนวนหลักสูตรที่เรียนจบ: `3/5 หลักสูตร`
- แสดงจำนวนหลักสูตรที่ลงทะเบียน: `ลงทะเบียน: 4/5`

#### **ตารางสรุป:**
- เพิ่มคอลัมน์ "หลักสูตรที่ลงทะเบียน" เพื่อแยกจาก "หลักสูตรที่เรียนจบ"

#### **การส่งออก CSV:**
- เพิ่มคอลัมน์ "หลักสูตรที่ลงทะเบียน" ในไฟล์ CSV

### 🎯 **ประโยชน์ที่ได้รับ**

1. **เห็นภาพรวมครบถ้วน**: ดูได้ว่าใครลงทะเบียนไปกี่หลักสูตรแล้ว
2. **ติดตามความก้าวหน้า**: รู้ว่าใครอยู่ในขั้นไหนของการเรียน
3. **วางแผนการตลาด**: สามารถ target กลุ่มที่ยังลงทะเบียนไม่ครบ
4. **การวิเคราะห์ที่ดีขึ้น**: ข้อมูลละเอียดขึ้นสำหรับการตัดสินใจ

### 🔧 **การใช้งาน**

1. เปิดหน้า Dashboard > โปรโมชั่น
2. เลือกโปรโมชั่น FTI Academy
3. ระบบจะแสดงข้อมูลผู้ลงทะเบียนทั้งหมด
4. ดูได้ว่าแต่ละคนลงทะเบียนกี่หลักสูตรและเรียนจบกี่หลักสูตร

### 📊 **ตัวอย่างสถิติที่แสดง**

```javascript
สถิติแคมเปญ FTI Academy: {
  'ผู้เข้าร่วมทั้งหมด': 1500,      // คนที่ลงทะเบียนอย่างน้อย 1 หลักสูตร
  'การลงทะเบียนรวม': 5200,       // จำนวนการลงทะเบียนทั้งหมด
  'เรียนครบ 5 หลักสูตร': 245,    // คนที่เรียนจบครบ
  'ใกล้จบ': 380,                 // คนที่ใกล้จบ
  'กำลังเรียน': 625,             // คนที่กำลังเรียน
  'เริ่มใหม่': 250                // คนที่เพิ่งเริ่ม
}
```

### 🚀 **การใช้งานต่อ**

ข้อมูลนี้สามารถนำไปใช้ใน:
1. **Email Marketing**: ส่งอีเมลเตือนให้ลงทะเบียนหลักสูตรเพิ่ม
2. **Push Notifications**: แจ้งเตือนผู้ที่ลงทะเบียนแต่ยังไม่เรียน
3. **Reward System**: ให้รางวัลตามจำนวนหลักสูตรที่ลงทะเบียน
4. **Analytics**: วิเคราะห์ pattern การลงทะเบียนและการเรียน