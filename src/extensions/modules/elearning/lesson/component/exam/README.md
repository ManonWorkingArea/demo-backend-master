# ExamRenderer Component

ExamRenderer เป็น component สำหรับ render แบบทดสอบแบบเลเวล ที่สามารถวัดทักษะในหมวดหมู่ต่างๆ และแสดงผลเป็นคะแนนเปอร์เซ็นต์

## การใช้งาน

### 1. Import Component

```vue
<template>
  <ExamRenderer 
    :examConfig="examConfig"
    :questions="questions"
    @answer-updated="handleAnswerUpdated"
    @exam-submitted="handleExamSubmitted"
    @exam-reset="handleExamReset"
  />
</template>

<script>
import ExamRenderer from './ExamRenderer.vue'
</script>
```

### 2. Props

#### examConfig (Object, Required)
ข้อมูลการตั้งค่าแบบทดสอบที่มาจาก ExamForm:

```javascript
{
  name: 'ชื่อแบบทดสอบ',
  description: 'คำอธิบาย',
  measurement_type: 'level',
  level_categories: [
    { id: 1, name: 'การอ่าน', code: 'reading', color: '#3B82F6' },
    { id: 2, name: 'การเขียน', code: 'writing', color: '#10B981' }
  ],
  level_structure: {
    max_percentage: 100,
    output_levels: 10,
    pass_percentage: 60,
    score_levels: [
      { id: 1, label: 'ไม่เข้าใจเลย', percentage: 0 },
      { id: 2, label: 'เข้าใจเล็กน้อย', percentage: 25 },
      { id: 3, label: 'เข้าใจดี', percentage: 75 },
      { id: 4, label: 'เข้าใจสมบูรณ์', percentage: 100 }
    ]
  }
}
```

#### questions (Array, Required)
ข้อสอบที่จะแสดง:

```javascript
[
  {
    id: 1,
    category_id: 1, // ต้องตรงกับ level_categories.id
    title: 'คำถาม',
    description: 'คำอธิบายเพิ่มเติม (optional)'
  }
]
```

### 3. Events

#### @answer-updated
เมื่อผู้ใช้เลือกคำตอบ:

```javascript
handleAnswerUpdated(data) {
  console.log(data);
  // {
  //   questionId: 1,
  //   scoreLevelId: 3,
  //   answers: { 1: 3, 2: 1, ... }
  // }
}
```

#### @exam-submitted
เมื่อส่งแบบทดสอบ (ตอบครบทุกข้อ):

```javascript
handleExamSubmitted(results) {
  console.log(results);
  // {
  //   userAnswers: { 1: 3, 2: 1, ... },
  //   categoryScores: {
  //     1: { name: 'การอ่าน', code: 'reading', score: 75.5, level: 8 }
  //   },
  //   overallScore: 72.3,
  //   passed: true
  // }
}
```

#### @exam-reset
เมื่อรีเซ็ตแบบทดสอบ

## Features

### 🎯 การจัดกลุ่มตามหมวดหมู่
- แสดงข้อสอบแยกตามหมวดหมู่ทักษะ
- แต่ละหมวดหมู่มีสีและความคืบหน้าแยกกัน

### 📊 การคำนวณคะแนน
- คำนวณคะแนนเฉลี่ยของแต่ละหมวดหมู่
- แปลงเป็นระดับ (1-N) ตาม output_levels
- คำนวณคะแนนรวมจากค่าเฉลี่ยของทุกหมวดหมู่

### 🎮 UI/UX Features
- Progress bar แสดงความคืบหน้า
- สถานะการตอบของแต่ละข้อ
- Scroll ไปข้อที่ยังไม่ได้ตอบ
- แสดงผลลัพธ์แบบ real-time

### 📈 ระบบการให้คะแนน
- ใช้ระดับคะแนนที่กำหนดเอง (score_levels)
- แต่ละคำตอบมีเปอร์เซ็นต์ที่แตกต่างกัน
- ยืดหยุ่นในการตั้งค่าระดับความยาก

## ตัวอย่างการใช้งาน

ดู `ExamDemo.vue` สำหรับตัวอย่างการใช้งานแบบสมบูรณ์

### การนำไปใช้ในโปรเจกต์

1. **ตั้งค่าแบบทดสอบ** ใน ExamForm.vue
2. **สร้างข้อสอบ** และกำหนด category_id ให้ตรงกัน
3. **ใช้ ExamRenderer** เพื่อให้ผู้ใช้ทำแบบทดสอบ
4. **รับผลลัพธ์** และนำไปสร้างกราฟแมงมุม

### การแปลงผลเป็นกราฟแมงมุม

```javascript
// ตัวอย่างการนำผลไปสร้าง spider chart
const spiderData = Object.values(results.categoryScores).map(cat => ({
  skill: cat.name,
  level: cat.level,
  maxLevel: examConfig.level_structure.output_levels
}));
```

## การพัฒนาต่อ

- เพิ่มการบันทึกความคืบหน้าแบบ auto-save
- เพิ่มตัวจับเวลาสำหรับแบบทดสอบ
- เพิ่มการแสดงผลแบบกราฟแมงมุม
- เพิ่มการ export ผลลัพธ์ 