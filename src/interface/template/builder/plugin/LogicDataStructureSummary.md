# 📋 Logic Configuration Data Structure Summary

## 🎯 **ตัวอย่างข้อมูลจริงจากผู้ใช้**

### **ที่ต้นทาง (Source Element):**
```javascript
// Element ที่ทำหน้าที่เป็นตัวควบคุม (เช่น RadioBox, Select)
{
  "uid": "source_element_001",
  "name": "ประเภทลูกค้า",
  "logics": [
    {
      "uid": "logic-001",
      "default": "offline-corporate",
      "destination": {
        "name": "หมายเลขประจำตัวผู้เสียภาษี",
        "rowIndex": 11,
        "columnIndex": 1,
        "obj": 0
      },
      "method": "show"
    },
    {
      "uid": "logic-002",
      "default": "offline-corporate",
      "destination": {
        "name": "สาขา (ระบุเป็น 0 หากเป็นสำนักงานใหญ่)",
        "rowIndex": 11,
        "columnIndex": 1,
        "obj": 1
      },
      "method": "show"
    },
    {
      "uid": "logic-003",
      "default": "offline-corporate",
      "destination": {
        "name": "ชื่อนิติบุคคล",
        "rowIndex": 11,
        "columnIndex": 2,
        "obj": 0
      },
      "method": "show"
    },
    {
      "uid": "logic-004",
      "default": "offline-corporate",
      "destination": {
        "name": "ที่อยู่ (สำหรับนิติบุคคล)",
        "rowIndex": 11,
        "columnIndex": 3,
        "obj": 0
      },
      "method": "show"
    }
  ]
}
```

### **ที่ปลายทาง (Target Element):**
```javascript
// Element ที่ถูกควบคุม (เช่น Input, TextArea)
{
  "uid": "target_element_001",
  "name": "หมายเลขประจำตัวผู้เสียภาษี",
  "logics": [
    {
      "request": true,
      "default": "offline-corporate",
      "method": "show",
      "rowIndex": 11,
      "columnIndex": 0,
      "objectIndex": 0,
      "_metadata": {
        "uid": "request-001",
        "parentUid": "source_element_001",
        "parentName": "ประเภทลูกค้า",
        "sourceLogicUid": "logic-001",
        "targetRowIndex": 11,
        "targetColumnIndex": 1,
        "targetObjIndex": 0
      }
    }
  ]
}
```

## 🔧 **Key Properties อธิบาย:**

### **Source Element (ต้นทาง):**
- `logics[]` - Array ของ logic rules ที่ควบคุม destinations
- `destination.name` - ชื่อของ element ปลายทาง
- `destination.rowIndex` - ตำแหน่งแถวของ element ปลายทาง
- `destination.columnIndex` - ตำแหน่งคอลัมน์ของ element ปลายทาง
- `destination.obj` - ตำแหน่ง object ในคอลัมน์ของ element ปลายทาง
- `default` - ค่าที่ใช้เปรียบเทียบ (เช่น "offline-corporate")
- `method` - วิธีการแสดงผล ("show" หรือ "hide")

### **Target Element (ปลายทาง):**
- `request: true` - ระบุว่าเป็น request object
- `default` - ค่าที่ใช้เปรียบเทียบ (เดียวกับ source)
- `method` - วิธีการแสดงผล (เดียวกับ source)
- `rowIndex` - ตำแหน่งแถวของ **source element**
- `columnIndex` - ตำแหน่งคอลัมน์ของ **source element**
- `objectIndex` - ตำแหน่ง object ของ **source element**
- `_metadata` - ข้อมูลเพิ่มเติมสำหรับ debugging และ tracking

## 🔄 **การทำงานของ Logic System:**

1. **When User Selects Value**: ผู้ใช้เลือกค่าใน Source Element
2. **Value Comparison**: ระบบเปรียบเทียบค่าที่เลือกกับ `default` ใน logic rules
3. **Apply Logic**: ถ้าค่าตรงกัน ระบบจะใช้ `method` เพื่อแสดง/ซ่อน Target Elements
4. **Bidirectional Sync**: ข้อมูลจะถูกอัปเดตทั้งที่ Source และ Target

## 🎨 **Example Use Case:**
```
ประเภทลูกค้า: [บุคคลธรรมดา] [นิติบุคคล]

เมื่อเลือก "นิติบุคคล" (offline-corporate):
✅ แสดง: หมายเลขประจำตัวผู้เสียภาษี
✅ แสดง: สาขา
✅ แสดง: ชื่อนิติบุคคล  
✅ แสดง: ที่อยู่นิติบุคคล

เมื่อเลือก "บุคคลธรรมดา":
❌ ซ่อน: หมายเลขประจำตัวผู้เสียภาษี
❌ ซ่อน: สาขา
❌ ซ่อน: ชื่อนิติบุคคล
❌ ซ่อน: ที่อยู่นิติบุคคล
```

## 🚀 **Implementation Status:**
- ✅ Bidirectional Data Sync
- ✅ Builder Format Compatibility
- ✅ Render Format Compatibility
- ✅ Multiple Destinations Support
- ✅ Debug Metadata
- ✅ Error Handling
- ✅ Backward Compatibility

## 📝 **Notes:**
- ระบบจะสร้าง request objects อัตโนมัติเมื่อสร้าง logic rules
- การลบ logic rules จะลบ request objects ที่เกี่ยวข้องด้วย
- ข้อมูล metadata จะช่วยในการ debug และ tracking
- รองรับ multiple destinations ต่อ 1 source element 